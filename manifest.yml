---
applications:
  - name: appdnozzle
    buildpack: https://github.com/cloudfoundry/go-buildpack.git
    health-check-type: process
    env:
      GOVERSION: go1.8
      NOZZLE_UAA_URL: https://uaa.<domain>
      NOZZLE_TRAFFIC_CONTROLLER_URL: wss://doppler.<domain>:443
      NOZZLE_USERNAME: opentsdb-firehose-nozzle
      NOZZLE_PASSWORD: <password for opentsb account>
      NOZZLE_FIREHOSE_SUBSCRIPTION_ID: appdynamics.firehose
      NOZZLE_SKIP_SSL: true
      NOZZLE_SELECTED_EVENTS: ValueMetric
      NOZZLE_SINK: Controller # one of the following STDOUT|MACHINEAGENT|CONTROLLER|SPLUNK etc...
      APPD_CONTROLLER: <controller>
      APPD_ACCESSKEY: <accesskey>
      APPD_PORT: <port>
      APPD_ACCOUNT: <account>
      LD_LIBRARY_PATH: /app/vendor/appdynamics/lib
